import React from "react"
import './styles/locationInfoBox.css'

const LocationInfoBox = ({ title }) => {
    return (
        <div className="location-info"> {title}</div> 
    )
}

export default LocationInfoBox